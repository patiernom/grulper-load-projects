{
  "name": "grulper-load-projects",
  "version": "1.0.0",
  "description": "A simple Grulper module for load your projects specification from JSON.",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/patiernom/grulper-load-projects.git"
  },
  "keywords": [
    "gulp"
  ],
  "author": "Marco Patierno",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/patiernom/grulper-load-projects/issues"
  },
  "homepage": "https://github.com/patiernom/grulper-load-projects#readme",
  "main": "index.js",
  "scripts": {
    "test": "gulp test",
    "coveralls": "istanbul cover ./node_modules/mocha/bin/_mocha $(find tests -name '*.js') --report lcovonly -- -R spec && cat ./coverage/lcov.info | ./node_modules/coveralls/bin/coveralls.js && rm -rf ./coverage"
  },
  "engines": {
    "node": ">=0.10.22"
  },
  "dependencies": {
    "minimist": "^1.1.3"
  },
  "devDependencies": {
    "chai": "^3.2.0",
    "coveralls": "^2.11.2",
    "gulp-istanbul": "^0.10.0",
    "gulp-jscs": "^1.6.0",
    "gulp-jshint": "^1.11.2",
    "gulp-load-plugins": "^0.10.0",
    "gulp-mocha": "^2.1.3",
    "istanbul": "^0.3.17",
    "jshint-stylish": "^2.0.1",
    "mocha": "^2.2.5",
    "mocha-lcov-reporter": "0.0.2",
    "rewire": "^2.3.4"
  },
  "peerDependencies": {
    "gulp": "^3.9.0"
  }
}
